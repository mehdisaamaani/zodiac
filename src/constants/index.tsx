export const USER_SETTING = "user-setting";

export const TOKEN_COOKIE_NAME = "user_token";

export const LAYOUT_WIDTH: Record<"xs" | "md", `${number}px` | `${number}%`> = {
  xs: "100%",
  md: "480px",
};
